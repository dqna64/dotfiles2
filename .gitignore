# === macOS
.DS_Store
.AppleDouble

# === Windows
Thumbs.db
Desktop.ini

# === Vim
*.swp
*.swo
*~

# === VSCode
.vscode/settings.json

# === JetBrains IDEs
.idea/

# === General
.env.local

# === Dotfiles-repo-specific

# Per-machine zsh config, bootstrapped from zsh/zsh-config.example by install.sh.
# Each machine edits this independently — must not be committed.
zsh/zsh-config

# Local git identity (real name/email/SSH key paths), bootstrapped from
# git/git-identity.example.
git/git-identity

# Rendered per-machine config files (contain real values substituted from
# git-identity). Live next to their templates and are written by
# git/git-setup.sh.
git/dqna64-dotfiles.gitconfig
ssh/dqna64-dotfiles.conf
